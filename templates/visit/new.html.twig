{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(visitForm) }}


        {{ form_label(visitForm.date) }}
        <div class="col-sm-10">
            {{ form_widget(visitForm.date) }}
        </div>

    {{ form_end(visitForm) }}

    {% endblock %}

    {% block javascripts %}

    {{ parent() }}

    <script type="text/javascript">
        $(function() {
           $('#product_datetime').datetimepicker({
               inline: true,
               sideBySide: true,
               showTodayButton: true,
               toolbarPlacement: 'bottom',
               format: 'YYYY HH:mm:ss DD-MM'
           });
        });
    </script>

{% endblock %}
